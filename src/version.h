/* The version string.  CMake defines DXM_VERSION from project(); a build
 * that bypasses CMake gets "dev" so the banner and the log still say
 * something true. */
#ifndef DXM_VERSION_H
#define DXM_VERSION_H
#ifndef DXM_VERSION
#  define DXM_VERSION "dev"
#endif
#endif
